# 감사 결과 검증·조치 기록 (2026-09-24)

지시서: `prompt/Claude용 감사 결과 검증 및 조치 프롬프트.md` · 보고서: `docs/audit/2026-09-24-전체-코드-보안-안정성-감사.md`
재현 스크립트: `PYTHONPATH=. .venv\Scripts\python.exe docs/audit/verify_audit_findings.py` — 착수 시점 14건 중 14건 "재현됨".

## 항목별 기록

```text
[CRITICAL-1] 주문 응답 타임아웃 후 identifier 조회 실패/404 → 새 identifier 로 재주문 (중복 주문)
판정: 진짜 오류
재현 방법과 결과: 스크립트 [CRITICAL-1] 두 줄 모두 "재현됨: 거래소 주문 2건 ['c1', 'c1-r2'], 내부 상태 FILLED".
  tests/test_live_broker.py::test_network_error_without_order_is_rejected 는 "1차·2차 모두 서버 생성 실패" 만 다뤄
  "서버 생성 성공 + 응답 유실 + 조회 실패" 를 놓치고 있었음. 공식 문서(reference/get-order) 로 없는 주문은 404 order_not_found 임을 확인.
조치: app/trading/live_broker.py — 네트워크 오류 뒤 새 identifier 재주문 경로 제거. 같은 identifier 로만 백오프(1·2·4·8초, 최대 5회)
  재조회하는 _confirm_by_identifier 추가: 조회되면 그 주문을 이어서 처리, 모든 시도가 404 이면(간격을 둔 뒤에만) 미생성으로 거부,
  조회 자체가 한 번이라도 실패하면 OrderStatus.UNKNOWN 으로 기록하고 client_id 를 processed 에 넣어 같은 신호 재주문 차단.
  app/trading/orders.py — OrderStatus.UNKNOWN, Order.exchange_identifier 추가. app/trading/engine.py — UNKNOWN 주문은 계좌를 바꾸지 않고
  order_unknown(ERROR) 로그·알림·stats.orders_unknown 만 기록.
회귀 테스트: tests/test_live_broker.py::TestBuy::test_network_error_then_lookup_failure_marks_unknown_without_reorder,
  ::test_network_error_then_mixed_404_and_timeout_stays_unknown, ::test_network_error_then_delayed_visibility_adopts_existing_order,
  ::test_network_error_without_order_is_rejected(새 동작으로 수정: 재조회 3회 + 백오프 확인),
  tests/test_engine_control.py::test_unknown_order_is_recorded_without_touching_portfolio
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [CRITICAL-1] 두 줄 "재현 안 됨" (주문 1건, UNKNOWN 또는 REJECTED)
남은 위험: UNKNOWN 주문의 사후 확정(uuid·identifier 재조회로 FILLED 반영)과 같은 마켓 재주문 차단은 HIGH-1 에서 처리.
  duplicated_identifier 응답 시 새 접미사로 재시도하는 경로는 감사 범위 밖이라 유지(조치 범위 최소화).
```

```text
[CRITICAL-2] 오래된 호가를 mark_price 로 사용 → WebSocket 장애 중 손절·일일 손실·괴리 검사 무력화
판정: 진짜 오류
재현 방법과 결과: 스크립트 [CRITICAL-2] 두 줄 "재현됨" (is_fresh=True 인데 mark_price 가 3시간 전 호가 1억, 실제 -20% 에서 손절 None).
  코드 확인: engine.ensure_prices 는 set_last_price 만 호출해 best_bid/ask·book_time 을 갱신·무효화하지 않았고,
  is_fresh 는 max(last_time, book_time) 기준이라 "체결가는 방금, 호가는 몇 시간 전" 이 통과했음.
조치: app/trading/market_state.py — PriceState.book_max_age_seconds(기본 30초)와 book_usable 추가: 호가가 최신 체결가(REST 보정 포함)
  시각보다 그만큼 넘게 오래됐으면 mark_price 가 호가를 버리고 last_price 를 쓴다. MarketState.price_max_age_seconds 로 전달,
  엔진은 settings.price_max_age_seconds 를 넘긴다. is_fresh 는 "실제로 쓰는(더 최신) 데이터의 시각" 과 일치하므로 유지.
  PaperBroker 는 이미 book_time 을 절대 시각으로 검사하고 있어 변경 없음(tests/test_orders.py 통과).
회귀 테스트: tests/test_price_freshness.py::test_stale_book_falls_back_to_last_price, ::test_market_state_passes_max_age_to_price_state,
  ::test_engine_stop_loss_uses_rest_price_when_book_is_stale (호가 3시간 전 + REST 보정 -27% → 손절 체결).
  tests/test_market_state.py::TestPrices::test_updates_from_ws_messages 는 고정 데이터의 호가가 현재가보다 18분 전이라
  새 규칙대로 "무시됨" 을 확인한 뒤 같은 시각 호가로 중간값을 검증하도록 수정.
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [CRITICAL-2] 두 줄 "재현 안 됨"
남은 위험: 시세가 N초 이상 끊겼을 때의 알림·신규 진입 중단은 HIGH-2(시세 루프 재생성)에서 함께 다룬다.
```

```text
[HIGH-1] 주문 상태에 UNKNOWN/PARTIAL 이 없어 "체결 확인 실패" 가 REJECTED 로 축약됨
판정: 진짜 오류
재현 방법과 결과: 스크립트 [HIGH-1] "재현됨: 거래소 주문 1건 생성, 내부 상태 REJECTED, 내부 현금 1,000,000".
  코드 확인: _wait_for_fill 실패 → REJECTED, 엔진은 아무 일도 없던 것으로 처리 → 체결된 매수는 손절 감시 밖, 체결된 매도는 1초마다 재시도.
조치: app/trading/orders.py — OrderStatus 에 SUBMITTED / PARTIAL / CANCELLED 추가 (UNKNOWN 은 CRITICAL-1 에서), is_filled 는 FILLED·PARTIAL.
  DB orders.status 는 String(12) 라 PARTIALLY_FILLED 대신 PARTIAL(7자) 사용, orders.exchange_identifier 컬럼 추가(구버전 DB 자동 보강).
  app/trading/live_broker.py — 체결 확인 실패 → UNKNOWN(uuid 보존), 부분 체결 후 취소 → PARTIAL, 체결 없이 취소 → CANCELLED,
  resolve_order(): uuid(없으면 identifier)로 후속 조회해 최종 상태면 체결 반영, 대기 중이면 fill_timeout 경과 시 취소 접수, 404 면 미생성 확정.
  app/trading/engine.py — 미확정 주문 목록(pending_orders): 같은 마켓의 신규 신호·청산을 막고(pending_order_skip), 명령 폴링 주기(2초)마다
  resolve_pending_orders 로 확정, 600초 안에 확정 못 하면 order_unresolved 기록·알림 후 거래소 잔고 동기화(LIVE). 재시작 시 DB 의 UNKNOWN 주문을
  load_pending_orders 로 복구. 청산 주문 실패는 마켓별 백오프(5초→10초→…최대 300초)로 재시도 폭주를 막는다(_note_exit_result).
회귀 테스트: tests/test_live_broker.py::TestUnknownAndResolve (poll 실패→UNKNOWN, resolve 로 FILLED/PARTIAL/CANCELLED/대기 취소/조회 실패/404),
  ::TestBuy::test_unfilled_order_is_cancelled_after_timeout(새 동작 CANCELLED 로 수정),
  tests/test_engine_control.py::test_pending_unknown_order_blocks_market_until_resolved, ::test_pending_order_gives_up_after_max_age,
  ::test_load_pending_orders_from_db, ::test_exit_retry_backoff_prevents_storm
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [HIGH-1] "재현 안 됨" (UNKNOWN, 현금 미차감)
남은 위험: 확정 전 잠정 반영(reconcile 로 포지션 추정)은 넣지 않았다 — 확정 대기 중에는 해당 마켓을 건드리지 않는 쪽이 더 안전하다고 판단.
  identifier 일괄 조회(GET /v1/orders/uuids)는 미사용(개별 조회로 충분, 마켓당 미확정 1건).
```

```text
[HIGH-2] 시세 루프가 예외 한 번에 영구 종료되고 재시작하지 않음
판정: 진짜 오류
재현 방법과 결과: 스크립트 [HIGH-2] "재현됨: 시세 루프 종료=True, price_stream_failed 로그=True" (check_exits 안의 save_order 가
  OperationalError 를 내자 루프 코루틴이 끝남). 코드 확인: _price_loop 의 except Exception 이 로그만 남기고 return, 재생성 없음.
조치: app/trading/engine.py — _handle_price_message 로 메시지 단위 예외 격리(exit_check_failed 기록·알림, 다음 시세에서 재시도),
  _price_loop 를 while 루프로 바꿔 스트림 종료·예외 시 백오프(1→2→…60초) 후 ws_factory 로 재연결(price_stream_restarts 통계),
  _log_safely 로 DB 기록 실패가 루프를 죽이지 않게. 메인 루프의 명령 폴링 주기마다 _check_price_staleness: 포지션이 있는데
  120초 넘게 시세가 없으면 price_stale 기록·알림(복구 시 리셋).
회귀 테스트: tests/test_engine_control.py::test_price_loop_survives_exit_check_exception, ::test_price_loop_reconnects_after_stream_error,
  ::test_price_staleness_alert_once_until_recovered
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [HIGH-2] "재현 안 됨" (루프 유지, price_stream_failed 없음)
남은 위험: 재연결이 계속 실패하면 60초 간격으로 무한 재시도하며 그동안 REST 보정(캔들 경계·스냅샷 주기)에만 의존한다 — price_stale 알림으로 드러난다.
```

```text
[HIGH-3] 재시작 시 일일 손실 한도 기준선이 리셋되고 halt·잠금이 사라짐
판정: 진짜 오류
재현 방법과 결과: 스크립트 [HIGH-3] 두 줄 "재현됨" (재시작 후 day_start=975,000 → 당일 -5% 에서 lock=None; halt 후 재시작 halted=False).
  코드 확인: rebuild 가 RiskState() 로 초기화하고 start_day_if_needed(now, equity) 로 현재 자산을 당일 시작값으로 씀. halted/lock_reason/last_exit_at 는 메모리에만.
조치: app/risk/state_store.py(신규) — RiskStateStore 프로토콜, MemoryRiskStateStore(프로세스 전역 기본값), RepositoryRiskStateStore(DB).
  app/risk/manager.py — RiskManager(config, store): 상태 전이(당일 시작·일일/연속 손실 잠금·halt/resume·거래 반영)마다 저장,
  rebuild 는 저장값이 오늘이면 day_start_equity·일일 손실 잠금·last_exit_at 을 복구하고, halt 는 날짜와 무관하게 resume 전까지 유지,
  연속 손실 잠금·당일 손익은 DB 의 오늘 거래를 다시 세어 계산(lock_kind 로 구분해 이중 반영·오래된 잠금 부활 방지).
  app/database — risk_state 테이블(모드별 1행) + Repository.save_risk_state/load_risk_state. app/main.py run — RepositoryRiskStateStore(repo) 주입.
  app/trading/engine.py — 복구 시 차단 상태면 risk_lock_restored 기록·알림. tests/conftest.py — 전역 메모리 저장소를 테스트마다 비움.
회귀 테스트: tests/test_risk_persistence.py (스크립트와 같은 프로세스 내 재시작, 일일 손실 잠금 복구·다음 날 해제, 연속 손실 잠금은 거래로 재계산,
  저장소 격리, DB 저장소 왕복, 엔진 재시작 복구, 저장소 장애 시 무중단) 7건
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [HIGH-3] 두 줄 "재현 안 됨"
남은 위험: 추적 손절용 peak_prices 는 저장하지 않아 재시작 후 최고가가 현재가부터 다시 시작한다(보수적으로 손절이 늦어질 수 있음).
  스크립트가 요구하는 "같은 프로세스에서 새 RiskManager 로 복구" 는 전역 메모리 저장소로 충족했고, 실제 프로세스 재시작은 DB 저장소가 담당한다.
```

```text
[HIGH-4] 정체 후 누락 캔들의 오래된 신호를 현재가로 일괄 실행, 캔들 공백 미검출
판정: 진짜 오류
재현 방법과 결과: 스크립트 [HIGH-4] "재현됨: 3시간 정체 후 신호 [(10, BUY), (11, SELL), (12, HOLD)] → 주문 [SELL FILLED, BUY FILLED]".
  코드 확인: process_closed_candles 가 새로 닫힌 캔들마다 _on_signal → 즉시 주문, merge_candles 는 연속성 검사 없음(refresh_candles=5 초과 공백 영구).
조치: app/trading/engine.py — 한꺼번에 닫힌 캔들 중 마지막 캔들의 신호만 실행, 이전 캔들 신호는 _skip_stale_signal 로 저장·기록만
  (stale_signals_skipped 통계, 실행 가능한 신호면 stale_signal_skipped 로그). 마지막 캔들도 닫힌 지 한 인터벌+유예를 넘겼으면 실행하지 않음.
  app/trading/market_state.py — find_gaps(고정 길이 캔들의 연속성 검사). 엔진은 공백이 있으면 _refill_gaps 로 공백 시작부터 지금까지를
  덮을 만큼(최소 워밍업 분량) 다시 받아 합치고 candle_gap 로그를 남긴다.
회귀 테스트: tests/test_candle_catchup.py::test_stall_executes_only_last_candle_signal(스크립트 시나리오), ::test_last_candle_signal_executes_when_fresh,
  ::test_stale_last_candle_is_not_executed, ::test_candle_gap_is_refilled
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [HIGH-4] "재현 안 됨" (주문 0건)
남은 위험: 건너뛴 신호 사이에 실제로는 매수→매도가 있었다면 그 왕복은 놓친다(의도된 보수적 동작). 1w/1M/1y 캔들은 공백 검사 대상이 아니다.
```

```text
[HIGH-5] 대시보드 인증: 조회 API 무인증, IP 기반 인증의 CSRF·프록시 우회
판정: 진짜 오류
재현 방법과 결과: 스크립트 [HIGH-5] 두 줄 "재현됨" (외부 Origin 폼 POST 로 stop/halt/start 가 200, live 큐에 ['stop','halt'];
  토큰 설정 상태에서 /api/status·logs·settings·orders 가 200).
조치: app/api/server.py — 라우트별 require_auth 의존성을 없애고 /api/* 전체에 미들웨어 _guard 적용:
  토큰 설정 시 조회 포함 모든 요청에 X-Auth-Token 헤더 필수(hmac.compare_digest 상수 시간 비교, 실패 시 0.3초 지연,
  쿼리스트링 토큰 폐지), 토큰 없으면 로컬 호스트만. 변경·제어(POST/PUT/PATCH/DELETE)는 Sec-Fetch-Site 가 same-origin/none 이 아니거나
  Origin/Referer 가 대시보드 호스트와 다르면 403, 본문이 있는 요청은 application/json 만 허용(폼 POST CSRF 차단).
  /ws 는 접속 직후 첫 메시지 {"token": ...} 로 인증(URL 토큰 제거), 토큰 없으면 로컬만, mode 검증(LOW-14 겸).
  app/web/static/app.js — WebSocket 토큰을 첫 메시지로 전송. README — 프록시는 X-Forwarded-For 필수(uvicorn 은 127.0.0.1 프록시만 신뢰).
회귀 테스트: tests/test_api.py::test_token_auth(새 동작: 조회 401, 쿼리 토큰 401, WS 첫 메시지 인증·실패 4401),
  ::test_local_mode_rejects_cross_site_and_remote(외부 Origin 폼/JSON POST 403, Sec-Fetch-Site cross-site 403, 비JSON 403,
  같은 출처 200, 원격 IP 403, 원격 WS 4403). 본문 없는 POST 를 쓰던 기존 테스트는 json={} 로 수정.
검증: pytest 전체 통과 / ruff 0건 / 스크립트 [HIGH-5] 두 줄 "재현 안 됨"
남은 위험: 로컬 모드는 여전히 IP 만 믿는다 — 같은 PC 의 다른 사용자/프로세스는 막지 못한다(토큰 설정 권장). HTTPS·보안 헤더는 리버스 프록시 몫(MEDIUM-10).
```

```text
[HIGH-6] 비-TraderError 예외(DB 잠금 등)가 엔진 전체를 종료시키고 감시자·자동 재시작이 없음
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. run() 루프는 except TraderError 만 있어 poll_commands/write_heartbeat/전략 계산의 OperationalError·pandas
  예외가 그대로 전파됨을 확인. Harness 로 pending_commands 가 OperationalError 를 내게 하면 조치 전 run() 이 예외로 끝났음(회귀 테스트로 고정).
조치: app/trading/engine.py — 메인 루프의 모든 단계(명령 처리·설정 반영·미확정 주문·시세 점검·하트비트·시세 보정·청산 감시·캔들 처리·
  잔고 동기화·스냅샷)를 _guarded 로 감싸 어떤 예외도 루프를 끝내지 않게 하고 loop_step_failed 기록·알림, 실패 뒤 1초 백오프.
  연속 실패가 10회를 넘으면 안전 모드(safe_mode): 매수 신호 무시(safe_mode_skip), 청산 감시·명령 처리는 계속. 하트비트 기록은
  _write_status_safely 로 실패해도 진행(시작 직후 실패 포함), 종료 처리도 단계별로 실패를 삼켜 bot_stop 기록·알림·STOPPED 하트비트까지 진행.
  app/database/database.py — SQLite connect_args["timeout"]=30 (busy 대기, MEDIUM-9 겸). app/main.py — 예상 못 한 예외도 추적 로그 후 종료 코드 1.
회귀 테스트: tests/test_engine_control.py::test_run_survives_db_errors_in_loop_steps, ::test_heartbeat_db_failure_does_not_abort_start,
  ::test_repeated_failures_enter_safe_mode
검증: pytest 전체 통과 / ruff 0건 / (스크립트 항목 아님)
남은 위험: 프로세스 자체가 죽는 경우(OOM·강제 종료)의 자동 재기동은 Phase 10(systemd/Docker restart) 몫. 안전 모드는 재시작해야 풀린다.
```

```text
[HIGH-7] 엔진 단일 인스턴스 보장 없음
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. cmd_run 에 실행 중 엔진 검사 없음, api_bot_start 는 start_engine 호출 전 STARTING 을 기록하지 않음을 확인.
  조치 전 같은 모드 run 두 개가 동시에 떴다(수동 확인). 조치 후 두 번째 run 은 "같은 모드의 엔진이 이미 실행 중입니다 (pid·시작시각 …)"
  로 즉시 종료(코드 3).
조치: app/trading/instance_lock.py(신규) — 모드별 data/engine-{mode}.lock 에 OS 파일 잠금(Windows msvcrt / POSIX flock, 비대기).
  프로세스가 죽으면 OS 가 풀어 주므로 오래된 PID 파일 문제 없음. app/main.py — acquire_engine_lock 으로 run 시작 전 잠금, 종료 시 해제.
  app/api/server.py — Start 요청 시 start_engine 전에 STARTING 하트비트를 먼저 기록해 연타·동시 요청은 409.
회귀 테스트: tests/test_instance_lock.py (같은 파일 두 번째 잠금 실패·해제 후 성공·모드별 분리), tests/test_api.py::test_commands_and_start
  (시작 직후 STARTING 기록 → 재요청 409)
검증: pytest 전체 통과 / ruff 0건 / 실제 엔진 2개 동시 실행 시 두 번째 거부 확인
남은 위험: 잠금은 같은 PC 안에서만 유효하다. 다른 PC 에서 같은 API Key 로 엔진을 띄우는 것은 막지 못한다(하트비트 확인은 사람이).
```

```text
[MEDIUM-1] 시장가 매수 응답에 trades 가 없으면 평균가에 '주문 총액' 이 들어감
판정: 진짜 오류
재현 방법과 결과: 스크립트 [MEDIUM-1] "재현됨: 포지션 avg_price=100,000, 현금 차감=150" (총액 100,000원·체결 0.001 BTC 응답을
  trades 없이 _apply_fill 에 넣으면 단가 1억 대신 총액 10만원이 평균가가 됨). 공식 문서(reference/get-order, 2026-09-24 확인)로
  price 가 "지정가 주문의 경우 단가, 시장가 매수 주문의 경우 매수 총액" 이고 응답에 체결 금액 필드가 없음을 확인.
조치: app/exchange/models.py — OrderInfo.fill_funds(): trades 합계 → 없으면 ord_type 별로 price(시장가 매수·done)=총액,
  limit=단가×체결 수량, market(시장가 매도)·부분 체결 뒤 취소는 None. app/trading/live_broker.py — _refresh_trades 로 체결 목록이
  빌 때 개별 조회를 trades_lookup_attempts(기본 3)회 다시 받고, 그래도 금액을 모르면 _apply_fill 이 계좌에 반영하지 않고
  UNKNOWN 으로 남긴다(resolve_order 는 None 을 돌려 다음 조회에서 확정, 시한 초과 시 엔진이 잔고 동기화로 정리).
  docs/upbit-api-notes.md — price 의미·체결 금액 필드 부재 기록.
회귀 테스트: tests/test_live_broker.py::TestFillWithoutTrades (fill_funds 종류별 값, trades 없는 시장가 매수 → 단가 1억·현금 차감
  100,050원·재조회 횟수, trades 없는 시장가 매도 → UNKNOWN 유지 후 목록 도착 시 FILLED, 부분 체결 뒤 취소 → UNKNOWN 유지)
검증: pytest 404건 통과 / ruff 0건 / 스크립트 [MEDIUM-1] "재현 안 됨: 포지션 avg_price=100,000,000, 현금 차감=100,050"
남은 위험: 시장가 매수 done 은 총액을 전부 쓴 것으로 보므로 거래소가 돌려준 잔돈(최소 호가 단위 미만)만큼 평균가가 아주 조금
  높게 잡힐 수 있다(보수적 방향). 다음 reconcile 이 거래소 avg_buy_price 로 덮는다.
```

```text
[MEDIUM-2] avg_buy_price=0 · 먼지 잔고 포지션 편입 → 손절 무력화, 마켓 영구 점유, 매도 거부 반복
판정: 진짜 오류
재현 방법과 결과: 스크립트 [MEDIUM-2] 두 줄 "재현됨": avg_buy_price=0 인 BTC 를 편입하면 avg_price=0.0 이라 가격 1원까지 떨어져도
  check_exits 가 None(손절가 0), 1e-8 BTC(1원어치) 잔고도 has_position=True 로 편입돼 신규 매수를 막고 손절 매도가 거부(under_min_total_ask)됨.
조치: app/trading/live_broker.py — portfolio_from_accounts(reference_prices, min_order_amount): 평가액(현재가, 없으면 평균가)이 최소 주문
  금액 미만이면 포지션 대신 portfolio.dust 에 두고 경고; avg_buy_price=0 이면 현재가를 기준가로 삼고 Position.cost_known=False.
  reconcile(markets, prices): 이미 정한 기준가는 유지(시세로 매번 바뀌지 않게), diff 에 dust·cost_unknown 보고, 거래소가 평균가를 주면 복귀.
  app/risk/manager.py — check_exits 는 avg_price<=0 이면 처음 본 시세(high)를 기준가로 채우고 그 틱은 판정하지 않음(현재가 없이 복구된 경우 대비).
  app/trading/portfolio.py — Position.cost_known, Portfolio.dust, summary 에 cost_known. app/database — positions.cost_known 컬럼(자동 보강),
  저장·복구. app/main.py — LIVE 시작 시 현재가(get_tickers)를 기준가로 전달. app/trading/engine.py — 시작 시 ensure_prices 뒤 reconcile 에
  현재가 전달, 동기화 때마다 _warn_account_sync 로 account_sync 로그 + EventKind.ACCOUNT 알림(쿨다운 key). 대시보드 — 포지션 표에 "기준가" 표시.
  README(알림 표 account 행)·.env.example 갱신.
회귀 테스트: tests/test_live_broker.py::test_portfolio_from_accounts_excludes_dust_and_marks_unknown_cost,
  ::test_reconcile_keeps_reference_price_and_reports_dust; tests/test_risk.py::TestExits::test_unknown_cost_adopts_first_price_as_reference;
  tests/test_database.py::test_cost_known_flag_round_trips
검증: pytest 408건 통과 / ruff 0건 / 스크립트 [MEDIUM-2] 두 줄 "재현 안 됨" (avg_price=1.0 기준가 대체, 먼지 편입 False) → 14건 중 재현 0건
남은 위험: 기준가 기준 손익은 진짜 손익이 아니다(포켓 이전 코인의 원가는 봇이 알 수 없음). 평가액이 최소 주문 금액 아래로 내려간 실제 포지션은
  거래소 규칙상 팔 수 없어 포지션에서 빠지며 손절 대상이 아니다(잔고는 dust 로 보고됨).
```

```text
[MEDIUM-3] reconcile 이 locked 잔고를 무시해 진행 중 매도와 경합하면 포지션 삭제·왕복 기록 누락
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. portfolio_from_accounts 가 KRW·코인 모두 Account.balance(주문 가능)만 쓰고 locked 를 보지 않음을 확인 —
  시장가 매도가 접수돼 수량이 locked 로 옮겨간 순간 동기화가 돌면 exchange_qty=0 이라 내부 포지션을 지웠다(조치 전 동작을 회귀 테스트로 재현:
  balance 0·locked 0.01 → 포지션 삭제). 또 시세 루프의 청산 주문(execute)과 메인 루프의 동기화가 같은 이벤트 루프에서 await 사이에 끼어들
  수 있어, 체결 대기 중 받은 잔고 스냅샷이 체결 반영 뒤 포지션을 되살리는 반대 방향 경합도 가능했다.
조치: app/trading/live_broker.py — portfolio_from_accounts 가 balance+locked(Account.total)를 쓴다. reconcile(markets, prices, exclude):
  exclude(미확정 UNKNOWN 주문이 남은 마켓)의 포지션은 건드리지 않고 그때는 현금도 갱신하지 않으며 diff["skipped"] 로 알린다(확정 시 이중 반영
  방지). execute·resolve_order·reconcile 을 같은 asyncio.Lock 으로 직렬화(주문 체결 대기 중에는 동기화가 기다린다).
  app/trading/engine.py — 세 곳의 reconcile 호출에 exclude=set(self.pending_orders) 전달.
회귀 테스트: tests/test_live_broker.py::test_reconcile_counts_locked_balance_and_skips_pending_markets,
  ::test_execute_and_reconcile_do_not_interleave
검증: pytest 410건 통과 / ruff 0건 / (스크립트 항목 아님, 14건 중 재현 0건 유지)
남은 위험: 미확정 주문이 있는 동안은 현금이 갱신되지 않는다(최대 pending_max_age=600초, 이후 잔고 동기화로 정리). 사용자가 거래소에서 직접
  낸 지정가 주문의 locked 금액도 자산으로 세므로 주문 가능 현금보다 내부 현금이 클 수 있다(주문 크기는 거래소 가용 잔고로 다시 제한됨).
```

```text
[MEDIUM-4] 포켓 이전(입출금)이 누적 수익률·일일 손실 계산을 왜곡
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. performance() 가 equity/initial_cash−1, update_equity() 가 equity/day_start_equity 로 자산 변동 전부를
  손익으로 봄을 확인. 회귀 테스트로 조치 전 동작 재현: 봇→메인 10만원 이전 직후 일일 손실 한도(3%) 잠금이 걸리고(−10%), 메인→봇 20만원은
  수익 +20% 로 보였다.
조치: app/database — cash_flows 테이블(mode, time, amount(입금+/출금−), currency, note, source), Repository.save_cash_flow/load_cash_flows/
  net_cash_flow/max_cash_flow_id. app/api/server.py — 대시보드 포켓 이전(KRW)이 성공하면 LIVE 입출금으로 기록(메인→봇 +, 봇→메인 −).
  app/api/services.py — 누적 수익률 = equity/(initial_cash+순입출금)−1, 오늘 수익률은 오늘 입출금을 뺀 자산으로, 응답에 net_cash_flow·
  today_cash_flow. app/risk/manager.py — RiskState.day_cash_flow·cash_flow_cursor(영속), 일일 손실 기준 자산 = 당일 시작 자산 + 당일 입출금
  (daily_base_equity), apply_cash_flow/set_cash_flow_cursor, 날짜 바뀌면 당일 입출금 0, 잠금 사유에 입출금 반영 표기.
  app/trading/engine.py — 매 루프 apply_cash_flows: 커서 이후 기록을 기준 자산에 반영(cash_flow_applied 로그), 커서가 없으면(첫 실행·
  새 날짜) 기존 기록은 이미 자산에 포함된 것으로 보고 커서만 맞춤. README 포켓 탭 설명.
회귀 테스트: tests/test_risk.py::test_cash_flow_moves_daily_loss_base, tests/test_risk_persistence.py::test_cash_flow_state_survives_restart_same_day,
  tests/test_database.py::test_cash_flows_save_load_and_net, tests/test_cash_flows.py::test_engine_applies_dashboard_cash_flows,
  tests/test_api.py::test_pockets_endpoints(입출금 기록)·::test_performance_excludes_cash_flows
검증: pytest 415건 통과 / ruff 0건 / (스크립트 항목 아님, 14건 중 재현 0건 유지)
남은 위험: 거래소 앱에서 직접 옮긴 자산·코인 이전(KRW 외)은 기록되지 않아 그대로 손익으로 잡힌다. 누적 수익률은 시간가중이 아니라
  단순 기준 자산 조정이다.
```

```text
[MEDIUM-5] 강제 종료가 PID 재사용을 검증하지 않음
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. api_bot_kill 이 engine_status.pid 를 확인 없이 taskkill/os.kill 에 넘김을 확인. 회귀 테스트로 조치 전
  동작 재현: PID 가 다른 프로세스(notepad)를 가리켜도 kill_engine 이 호출됐다.
조치: app/api/process.py — process_cmdline(pid): OS 도구(Windows PowerShell Get-CimInstance Win32_Process / Linux /proc/pid/cmdline /
  macOS ps)로 명령줄을 읽고, 없으면 None·실패면 "". is_engine_process(cmdline): "app.main" 과 "run" 이 모두 있을 때만 엔진.
  app/api/server.py — 강제 종료 전 확인: 프로세스가 없으면 종료하지 않고 상태만 STOPPED 로 정리, 엔진이 아니거나 확인 실패면 409 로
  거부(dashboard_kill ERROR 로그에 명령줄 앞 200자), 엔진일 때만 kill. psutil 의존성은 추가하지 않음.
회귀 테스트: tests/test_api.py::test_kill_uses_pid(엔진이면 종료·재사용 PID 409·확인 실패 409·없는 프로세스는 STOPPED 정리),
  tests/test_process_guard.py(현재 프로세스 명령줄 조회, 없는 PID None, 엔진 판별 표식)
검증: pytest 417건 통과 / ruff 0건 / (스크립트 항목 아님, 14건 중 재현 0건 유지)
남은 위험: 하트비트가 STALE 인 엔진(멈춘 상태)도 명령줄만 맞으면 종료한다 — 강제 종료의 목적상 의도한 동작. 같은 PC 에서 다른 목적으로
  "python -m app.main run" 을 띄운 프로세스와는 구분하지 못한다(HIGH-7 의 모드별 잠금으로 같은 모드 엔진은 하나뿐).
```

```text
[MEDIUM-6] LiveBroker 가 시세 신선도·이상치를 보지 않고, 단일 이상 틱으로 손절이 발동
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. LiveBroker.execute 의 price 인자가 전혀 쓰이지 않아(PaperBroker 는 30초 넘은 시세를 거부) 시세 스트림·
  REST 보정이 모두 죽어도 시장가가 나감을 확인. 청산 감시는 1틱만 기준을 넘어도 즉시 시장가 매도였다(회귀 테스트로 조치 전 동작 재현).
조치: app/trading/live_broker.py — _price_guard: PriceState 가 있으면 price_max_age_seconds 안의 체결가·호가가 있어야 주문(아니면
  "시세 오래됨 — 주문 보류" 로 거부, processed 에 넣지 않아 다음 신호에서 재시도 가능), 시세 인자가 없으면 REST 현재가를 한 번 조회해
  시장이 살아 있는지 확인(실패하면 거부). app/trading/engine.py — 청산 조건이 연속 EXIT_CONFIRM_TICKS(기본 2)번 관측돼야 매도
  (_exit_streak, 조건이 풀리면 초기화). 캔들 경계의 강제 점검(force, REST 보정 체결가 기준)은 즉시 실행해 시세 스트림이 죽은 상태에서
  청산이 한 캔들 늦어지지 않게 함. app/config/settings.py — exit_confirm_ticks. README 설정표·.env.example.
회귀 테스트: tests/test_live_broker.py::TestPriceGuard(오래된 시세 거부, 시세 없음+REST 실패 거부 → REST 복구 시 진행, 신선하면 진행),
  tests/test_exit_confirm.py(1틱은 청산 안 함·되돌아오면 초기화, 연속 2틱이면 청산, 강제 점검은 즉시)
검증: pytest 423건 통과 / ruff 0건 / 스크립트 14건 중 재현 0건 유지
남은 위험: 이상치 자체(체결가 튐)를 판별하지는 않는다 — 연속 관측 요구로 1틱 튐만 걸러진다. 급락 시 시장가 슬리피지 상한(지정가 전환)은
  미구현. 시세 인자가 없고 클라이언트에 현재가 조회가 없는 경우(테스트 더블)는 통과한다.
```

```text
[MEDIUM-7] 체결 1건의 DB 기록이 여러 트랜잭션으로 나뉘어 중간 종료 시 상태 불일치 (PAPER)
판정: 진짜 오류
재현 방법과 결과: 코드 근거 항목. _record_order 가 save_order → save_fill → save_round_trip → sync_portfolio 를 각각 별도 세션으로 실행함을
  확인(주문 FILLED 저장 뒤 sync_portfolio 전에 죽으면 재시작 시 현금·포지션은 이전 값, client_id 는 처리됨). 회귀 테스트로 조치 후
  중간 실패 시 주문·체결·계좌가 모두 롤백됨을 확인.
조치: app/database/repository.py — record_fill(order, trades, portfolio): 주문(merge)·체결·왕복 거래·계좌·포지션 저장을 세션 하나로.
  기존 save_order/save_fill/save_round_trip/sync_portfolio 는 같은 레코드 빌더(_order_record 등)를 쓰도록 정리.
  app/trading/engine.py — 체결된 주문은 record_fill 로 한 번에 저장, UNKNOWN·거부 주문은 save_order 만.
회귀 테스트: tests/test_database.py::test_record_fill_is_atomic(중간 실패 → 아무것도 안 남음, 성공 → 전부 저장·복구),
  tests/test_engine_control.py FlakyRepo 가 record_fill 실패도 재현(HIGH-2 테스트 유지)
검증: pytest 424건 통과 / ruff 0건 / 스크립트 14건 중 재현 0건 유지
남은 위험: 이후의 order_filled 로그·스냅샷·리스크 상태 저장은 별도 트랜잭션이다(실패해도 계좌 정합성에는 영향 없음). LIVE 는 재시작 시
  잔고 동기화로 다시 맞춰지므로 영향이 더 작다.
```

## 미조치 항목 (사용자 지시로 MEDIUM-1 까지 진행, 2026-09-24)

```text
[MEDIUM-9] SQLite 잠금 대기 미설정
판정: 진짜 오류 → HIGH-6 조치에서 함께 해결 (connect_args["timeout"]=30, sqlite3 의 busy 대기와 동일 효과).

[MEDIUM-10] 토큰 인증 강도 부족
판정: 진짜 오류 → HIGH-5 조치에서 대부분 해결 (헤더 전용 토큰, hmac.compare_digest, 실패 시 0.3초 지연, URL 토큰 제거).
남은 것: 실패 횟수 잠금, CSP·X-Frame-Options·nosniff 헤더, HTTPS 강제 — 외부 노출 전 리버스 프록시에서 처리하거나 추가 조치.

[MEDIUM-11] key 없는 알림 쿨다운 없음
판정: 코드 근거 항목, 미검증. HIGH-1 의 청산 재시도 백오프(5초→최대 300초)로 폭주 원인은 완화됐지만 쿨다운 자체는 미조치.

[MEDIUM-8, 12, 13] 백테스트 추적손절 낙관 · 백테스트 Rate limit · 엔진 캔들 검증
판정: 코드 근거 항목, 검증·조치 안 함 — 보류.

[MEDIUM-14] Rate limit 외부 의존 (Retry-After 미지원 등)
판정: 검증 불가 — 문서 원문(reference/rate-limits) 대조 필요. 미조치.

[LOW-11] ruff 오류 10건
판정: 진짜 오류 → 이전 커밋들에서 정리돼 현재 ruff 0건.

[LOW-14] /ws 의 mode 미검증
판정: 진짜 오류 → HIGH-5 조치에서 mode 검증 추가(4400). 접속당 2초 DB 폴링은 그대로.

[LOW-1 ~ 10, 12, 13]
판정: 검증·조치 안 함 — 보류.
```

## 요약

```text
=== 조치 결과 ===
진짜 오류로 확인·수정: 10건 (CRITICAL-1, CRITICAL-2, HIGH-1, HIGH-2, HIGH-3, HIGH-4, HIGH-5, HIGH-6, HIGH-7, MEDIUM-1)
  + 다른 항목 조치 중 함께 해결 3건 (MEDIUM-9, LOW-11, LOW-14 mode 검증), 부분 해결 1건 (MEDIUM-10)
오탐: 0건
검증 불가·보류: 24건 — MEDIUM-2 (재현됨, 사용자 지시로 보류 · 조치안 설계 완료), MEDIUM-3 · 6 (코드로 오류 확인, 보류),
  MEDIUM-4 · 5 · 7 · 8 · 11 · 12 · 13 (코드 근거, 미검증), MEDIUM-14 (문서 원문 필요), LOW-1 ~ 10 · 12 · 13 (보류)
verify_audit_findings.py: 재현됨 14건 → 2건 (MEDIUM-2 두 줄만 남음)
pytest: 404건 통과 / ruff: 오류 0건
실거래 전 남은 필수 항목:
1. MEDIUM-2 — 먼지 잔고 제외·avg_buy_price=0 코인의 기준가 처리 (포켓 이전으로 코인을 넣을 계획이면 특히)
2. MEDIUM-3 — reconcile 의 locked 반영·진행 중 주문 마켓 제외 (청산 매도와 5분 동기화가 겹치면 왕복 기록 누락)
3. MEDIUM-6 — LiveBroker 시세 신선도 검사, 청산의 단일 틱 발동 완화
4. MEDIUM-4 — 포켓 이전이 일일 손실 한도·수익률에 섞이지 않게 (대시보드 포켓 이전을 쓰면 잠금 오발동)
5. 실서버 소액 실주문으로 UNKNOWN → 확정 경로와 trades 지연(재조회) 동작 확인, 대시보드를 외부에 열 경우 MEDIUM-10 잔여(HTTPS·보안 헤더·시도 제한)
```
